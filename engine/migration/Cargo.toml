[package]
name = "migration"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
publish = false

[dependencies]
async-trait = "0.1"
sea-orm-migration = { version = "2.0", features = ["sqlx-postgres", "runtime-tokio-rustls"] }
