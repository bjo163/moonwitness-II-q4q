# Supabase Migration Source Control

Runtime project: `q4q`

The repository contains the current security-hardening migration:

`supabase/migrations/20260921190940_harden_quran_core_security_v1.sql`

The runtime also contains earlier migrations that predate repository synchronization. Their names are recorded in `docs/SUPABASE_MIGRATION_LEDGER.md` when known.

Do not reconstruct historical migration SQL from names alone. For future changes, author and verify an explicit migration, then commit the exact SQL here.
